<!-- msw: complete this note before release; remove this line when it is done. -->
# {version}: {slug} ({date})

`{release_name}` (built as `{build_name}`): {source_label} plus tracked revisions by {author}.
Revision ids and the revision date are in the build manifest. Scope: state which sections changed
and what did not (figures, tables, citations, numbers).

## Source: {source_label}

- File: `{source_path}`
- SHA-256: `{source_sha_short}`

## Manuscript (tracked)

- One line per change: where, old text, new text, revision ids.

## Verification

- Gate:
- Reject view equals the source:
- Citations and bibliography:
- Proof of the accepted view:
- Native Word QA:
- Independent review:

## Open items for the author

1.

## Not done

- EndNote, the table of contents and page numbers are refreshed by the author in Word
  (EndNote first, then the table of contents, then page numbers).
