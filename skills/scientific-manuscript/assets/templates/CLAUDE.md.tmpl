# {title}

@AGENTS.md

Read `AGENTS.md` before any work in this folder. It is this project's policy: nothing is deleted,
new versions only, tracked edits under the name "{revision_name}" through `msw.py build`.
